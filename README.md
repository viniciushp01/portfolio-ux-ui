# Vinicius Pereira — Portfolio

Personal portfolio site built with React, TypeScript, Vite, and Tailwind CSS.

## Run locally

**Prerequisites:** Node.js

```
npm install
npm run dev
```

## Build for production

```
npm run build
```

Outputs to `dist/`. Deployed on Vercel (see `vercel.json` for SPA rewrite config).
